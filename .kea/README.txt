This folder holds Kea-only CLI tokens. Never put another product’s token here.

supabase-access-token  — from the account that owns Kea Production (laubnngplqvsxokbfski)
netlify-auth-token     — from the Kea Netlify team (site keachat / kea.chat)

Both files are gitignored. One secret per file, no extra lines.
